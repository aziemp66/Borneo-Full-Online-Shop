<header>
    <div><a href="/">Borneo</a></div>
    <nav>
        <ul>
            <% if(locals.isAuth) { %>
            <li>
                <form action="/logout" method="post">
                    <input
                        type="hidden"
                        name="_csrf"
                        value="<%= locals.csrfToken %>"
                    />
                    <button>Logout</button>
                </form>
            </li>
            <% } %>
        </ul>
    </nav>
</header>
